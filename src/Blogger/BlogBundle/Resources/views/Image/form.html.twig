{# src/Blogger/BlogBundle/Resources/views/Image/form.html.twig #}

<form id="picture_form" class="blogger"  action="{{ path('BloggerBlogBundle_upload_image', { 'blog_id' : blog_id }) }}" method="post" {{ form_enctype(picture_form) }}>
    {{ form_rest(picture_form) }}
    <p>
        <input type="submit" value="Upload Image">
    </p>  
</form>